export const imageRegex = new RegExp(/img:[^ \n]+/);
export const titleRegex = new RegExp(/title:[\S ]+/i);
export const subtitleRegex = new RegExp(/subtitle:[^<>\n]+/);
export const linkRegex = new RegExp(/link:[^ ]\|[^ \n]+/);
